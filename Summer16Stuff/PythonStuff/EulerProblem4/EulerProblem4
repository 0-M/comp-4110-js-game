def flipNum(numToFlip,count):
    i = numToFlip
    num2 = 0
    while i != 0:
        num2 += (int(i % 10) * (10 ** count))
        count-= 1
        i //= 10
    return num2

i = 999
while i > 990:
    j = 999
    while j > 900:
         if flipNum(i * j,5) == i * j:
             print(i * j)
        #print(i," x ",j)
         j -= 1
    i -= 1